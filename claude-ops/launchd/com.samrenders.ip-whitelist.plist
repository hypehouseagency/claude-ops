<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.<user>.ip-whitelist</string>

    <!-- Fire on network state changes (resolv.conf is rewritten on each IP/DNS change) -->
    <key>WatchPaths</key>
    <array>
        <string>/etc/resolv.conf</string>
        <string>/var/run/resolv.conf</string>
    </array>

    <!-- Fallback: poll every 5 minutes to catch Wi-Fi switches WatchPaths may miss -->
    <key>StartInterval</key>
    <integer>300</integer>

    <!-- Prevent storm during network flap -->
    <key>ThrottleInterval</key>
    <integer>30</integer>

    <key>ProgramArguments</key>
    <array>
        <string>/bin/bash</string>
        <string>-c</string>
        <!-- Source env (AWS creds, PATH) then run the whitelist script -->
        <string>export PATH="/usr/local/bin:/opt/homebrew/bin:$PATH"; source "$HOME/.zshenv" 2>/dev/null; source "$HOME/.zprofile" 2>/dev/null; exec "$HOME/Projects/claude-ops/claude-ops/scripts/sam-ip-whitelist.sh"</string>
    </array>

    <key>StandardOutPath</key>
    <string>/tmp/ip-whitelist.out.log</string>

    <key>StandardErrorPath</key>
    <string>/tmp/ip-whitelist.err.log</string>

    <!-- Do not trigger on login -->
    <key>RunAtLoad</key>
    <false/>

    <!-- Oneshot per event, not a daemon -->
    <key>KeepAlive</key>
    <false/>
</dict>
</plist>
